# Runtime dependencies for the hardware wallet rebalancer.
# Requires Python >= 3.11.
#
# pyproject.toml is the source of truth for packaging; this file mirrors it so
# the app can be installed without the package itself:
#     python -m pip install -r requirements.txt
#
# Installing the project (python -m pip install -e .) already pulls these in
# and is what the README and CI use.
#
# The tests need nothing beyond the standard library.

Pillow>=10.0
python-dotenv>=1.0
requests>=2.31
urllib3>=2.0
